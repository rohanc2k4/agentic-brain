---
name: crystallize
description: Distill the current session's primary thread into a structured wiki page with diff-and-approve.
---

Invoke the `crystallize` skill. Follow its flow: scope the thread, propose a destination, draft the page with frontmatter + citations, show diff, write on approval, add wikilink to the relevant index, log the action.

Topic hint (if provided): $ARGUMENTS
