---
name: connect
description: Bridge two concepts using the repo's knowledge graph and content. Read-only.
---

Invoke the `connect` skill via the Skill tool and follow it exactly. The full workflow lives at `.claude/skills/connect.md`.

Arguments: two concepts, quoted if multi-word. Example: `/connect project-a course-b` or `/connect "offer one" "career decision"`.
