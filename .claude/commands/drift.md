---
name: drift
description: Compare stated intentions vs actual behavior over a window. Surfaces divergence; optional priorities.md edits behind approval gate.
---

Invoke the `drift` skill via the Skill tool and follow it exactly. The full workflow lives at `.claude/skills/drift.md`.

Arguments (optional): `--days N` (default 30), `--since YYYY-MM-DD`.
