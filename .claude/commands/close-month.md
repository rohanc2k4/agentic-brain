---
name: close-month
description: Produce the monthly rollup note at monthly/YYYY-MM.md
---

Invoke the `close-month` skill via the Skill tool and follow it exactly. The full workflow lives at `.claude/skills/close-month.md`.

Argument (optional): month in `YYYY-MM` form. Defaults to the current calendar month.
