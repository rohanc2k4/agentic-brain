---
name: graph
description: Query the repo knowledge graph and per-page confidence.
---

Invoke the `graph` skill. Subcommand + args: $ARGUMENTS

Run: `python3 .claude/skills/graph/query.py $ARGUMENTS`

Subcommands: list, node, inbound, outbound, around, low-confidence, broken, export.
