---
name: trace
description: Track how your thinking on a topic has evolved over time across git history + decisions/log + daily notes. Read-only.
---

Invoke the `trace` skill via the Skill tool and follow it exactly. The full workflow lives at `.claude/skills/trace.md`.

Argument: a topic (slug, page title, or free-text keyword). Example: `/trace some-project` or `/trace "offer one vs offer two"`.
