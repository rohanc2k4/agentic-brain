---
name: graduate
description: Scan recent daily notes for #idea captures, cluster, propose canonical destinations, draft + diff + write via /crystallize.
---

Invoke the `graduate` skill via the Skill tool and follow it exactly. The full workflow lives at `.claude/skills/graduate.md`.

Arguments (optional): `--days N` to change the window (default 14), `--daily YYYY-MM-DD` for a single-day scan.
